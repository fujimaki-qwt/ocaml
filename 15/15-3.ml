(* 問題 15.3 *)
(* 目的 : 自然数を1つ受け取り、その数以下の素数を返す（エラトステネスのふる） *)
(* prime : int  -> int list *)

(* sieve : int list -> int list *)

let rec sieve lst = match lst with
		[] -> []
	|	first :: rest ->
			[first]
			@ d

let rec isPrime n =
	let max = n in
		if max div n- = 0
			then false
		else


[2, 3, 4]